// 02. Declare variables to store your first name, last name, marital status, country and age and display them using interploation method.

const firstName = "Pankaj";

const lastName = "Bhadane";

const maritalStatus = "Single";

const country = "India";

const age = 22;

console.log(`First Name is ${firstName} Last name is ${lastName} marital Status is ${maritalStatus} nationality of ${country} and age is ${age}.`)